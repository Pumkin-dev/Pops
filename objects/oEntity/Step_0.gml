/// @description Insérez la description ici
// Vous pouvez écrire votre code dans cet éditeur
var detection = PlayerDetection();
// Si le joueur est dans le rayon de détection de l'objet
if (detection)
{
	global.obj_det = object_index
}

if (global.pause)
{
	image_index-= sprite_get_speed(sprite_index)/room_speed;;
	exit;
}